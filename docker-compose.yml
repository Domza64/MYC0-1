version: "3.8"
services:
  MYC0-1-app:
    image: "ghcr.io/domza64/myc0-1:master"
    ports:
      - "5000:5000"
    volumes:
      - </path/to/your/music/folder>:/app/music # Your local music folder
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
    name: MYC0-1-app-data
