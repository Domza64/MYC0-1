# HARP-1 FastAPI backend

Development:
Create virtual env
`python3 -m venv .venv`
Activate virtual env
`source .venv/bin/activate`
Install deps:
`pip install -r requirements.txt`
Run dev server:
`fastapi dev main.py`
